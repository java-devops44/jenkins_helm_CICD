pipeline{
    agent any 

    stages {

        stage('sonar quality check'){
    
            steps{

                script{


                    withSonarQubeEnv('My SonarQube Server') {


                        sh 'mvn clean package sonar:sonar'
                    }
                 }
            }
        }
    }
}