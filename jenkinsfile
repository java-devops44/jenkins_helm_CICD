pipeline{
    agent any 

    stages {

        stage('sonar quality check'){
    
            steps{

                script{


                    withSonarQubeEnv(credentialsId: 'sonarQube') {


                        sh 'mvn clean package sonar:sonar'
                    }
                 }
            }
        }
    }
}