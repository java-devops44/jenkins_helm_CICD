pipeline{
    agent any 

    stages {

        stage('sonar quality check'){
    
            steps{

                script{


                    withSonarQubeEnv(credentialsId: 'soanr_auth') {


                        sh 'mvn clean package sonar:sonar'
                    }
                 }
            }
        }
    }
}